#!/bin/bash

# نمایش پیام شروع
echo "🚀 در حال بررسی و نصب پیش‌نیازها..."

# نصب پکیج‌های تعریف شده در package.json
npm install

# بررسی موفقیت‌آمیز بودن نصب
if [ $? -eq 0 ]; then
    echo "✅ پیش‌نیازها با موفقیت نصب شدند."
    echo "🌐 در حال راه‌اندازی سرور..."
    
    # اجرای فایل اصلی سرور
    npm start
else
    echo "❌ خطا در نصب پیش‌نیازها. لطفاً لاگ‌ها را بررسی کنید."
    exit 1
fi
